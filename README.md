# 鸦珉.icu 个人主页

当前阶段：V1.6
当前小版本：V1.6.24
项目定位：个人主页 / 项目地图 / 公开频道入口

## 线上地址

Vercel 地址：

```txt
https://yamin-icu.vercel.app/
```

自定义域名：

```txt
https://鸦珉.icu/
```

后台配置时使用：

```txt
xn--idyr71g.icu
```

正式服当前状态：

- 主站已部署当前 `V1.6.15` 页面。

测试服 Vercel 地址：

```txt
https://yamin-icu-test.vercel.app/
```

测试服目标域名：

```txt
https://test.鸦珉.icu/
```

测试服后台配置时使用：

```txt
test.xn--idyr71g.icu
```

测试服当前状态：

- Vercel 测试项目：`yamin-icu-test`。
- 测试项目已部署当前 `V1.6.24` 页面。
- `test.xn--idyr71g.icu` 已绑定到 Vercel 测试项目。
- 测试服自定义域名 `https://test.xn--idyr71g.icu/` 已可访问。
- 测试服 `/admin` 已禁用线上保存，接口也会在测试服域名下拒绝写入，避免误写主站数据。

## 本地查看

```txt
npm.cmd run preview
```

默认打开：

```txt
http://127.0.0.1:4173/
```

当前对话里也可能使用临时预览地址：

```txt
http://127.0.0.1:4181/
```

## 修改内容

内容数据先维护在：

```txt
v1/content/site-data.mjs
```

里面包含：

- 项目区卡片。
- 施工中。
- 怪念头。
- 自媒体与视频账号入口。
- 左下角站点签名。

## 当前主页视觉基准（V1.4 桌面 / V1.5 手机 / V1.6 加载）

V1.4.0 作为当前主页视觉定稿基准。后续新增功能时，除非明确要求重新设计主页，否则优先保持这套视觉结构：

- 背景保持蓝色天空方向，使用较多低矮横向云，云从右往左飘。
- 主页主体保持固定 `2400 x 1200` 的 2:1 设计画布，外层整体等比例缩放；不同桌面窗口尺寸下，应让整张项目地图一起缩放，不要让内部字体、卡片、间距各自重排变形。
- 保持四区地图骨架：左上项目区、右上施工中、左下怪念头、右下频道入口区，蓝色分割线和外框比例不随意调整。
- 项目区保持三列：两张竖向大卡片 + 右侧三张横向卡片；大卡片、横卡、标题、图标、按钮和状态标签按当前比例维护，不再只微调单个字号导致整体比例漂移。
- 项目卡片圆角、玻璃感、阴影、白色卡片质感、右侧待办条和右下频道条的宽度节奏以当前版本为准。
- 大项目卡片链接按钮不显示右箭头，避免网址文本拥挤。
- 怪念头区保持漂浮标签风格：碰撞范围、标签不换行、随机大小和物理碰撞参数以 V1.4.0 为基准；后续只在明确反馈下微调。
- 首屏加载进度条、延后启动念头物理动画和 Canvas 云层的性能策略保持不变。
- V1.4.3 起右上标题显示为“施工中”，左下标题显示为“怪念头”；右下频道入口条更矮，怪念头碰撞上下边界保持同样外扩，横向空白项目卡标题略小。
- V1.4.4 起后台保存会绑定打开编辑台时的线上 revision；如果线上内容已变化，旧编辑台不能直接覆盖保存。
- V1.5 起手机端新增竖版长卷轴布局：项目区、施工中、怪念头、自媒体入口按顺序纵向展示；桌面端仍保持 V1.4 固定 2400 x 1200 横版项目地图与整体等比缩放规则。
- V1.5.1 起手机端关闭云层背景动画，项目卡片图标缩小，怪念头运动数量和速度下调，优先改善手机端流畅度。
- V1.5.2 起手机端项目大卡移除大图标，标题放大并改为更紧凑的偏横向排布；怪念头区高度放大，手机端初始可见念头恢复为 7 个，自媒体标题下划线隐藏。
- V1.5.3 起手机端项目卡片描述不再被状态标签挤窄，卡片内部竖向间距收紧；空白项目卡片采用和已有项目卡一致的手机端结构，自媒体标题下划线恢复到标题下方。
- V1.5.4 起手机端站点标识移动到页面顶部并放大；怪念头区高度继续放大，手机端念头碰撞边界使用更大的统一留白。
- V1.5.5 起手机端站点标识继续放大并改为更浅的蓝色；怪念头长标签限制最大宽度，碰撞留白更均衡；空白项目卡片小字不再硬换行。
- V1.5.6 起手机端怪念头池横向外扩，碰撞安全边距改小，让标签有更明显的左右游走空间，避免堆成竖线。
- V1.5.7 起测试服 `/admin` 禁用线上保存，后端接口在测试服域名下拒绝写入，避免误写主站数据。
- V1.5.8 起主页线上数据读取超时从 1.4 秒提高到 4 秒；如果读取失败或回退到静态数据，会显示“数据没有读取成功，请刷新重试。”。
- V1.5.9 起主页线上数据读取失败时，背景云层会切换为 `THIS IS FINE` 表情包背景；桌面端横向滚动，手机端保持静态以减少性能压力。
- V1.6.0 起首页新增全屏读取层，页面打开后先停留在“正在读取项目地图”的进度条状态；线上数据读取完成或回退数据完成渲染后，显示“读取完成”再露出主页。
- V1.6.0 的读取层退出时机放在关键内容渲染之后、怪念头物理动画和 Canvas 云层启动之前；怪念头运动可以继续延后，不阻塞主页露出。
- V1.6.1 起读取层区分成功和失败：只有真实读取到线上数据时显示“读取完成”；读取失败或回退到静态数据时显示“读取失败，显示备用页面”，进度条停在未满格状态，随后更快露出备用主页。
- V1.6.1 起加载面板和背景遮罩透明度降低，让读取层更轻、更透。
- V1.6.2 起读取失败路径会先保留 4 秒“正在读取项目地图”，4 秒内进度条线性推进到约 82%；如果仍是失败或静态回退，才切换为“读取失败，显示备用页面”。
- V1.6.2 起读取层里的“鸦珉.icu”站名放大，增强首屏品牌识别。
- V1.6.3 起读取失败提示出现后的停留时间从 760ms 缩短到 360ms，让备用主页更快露出。
- V1.6.4 起读取层进入完成状态时会取消 8.2 秒兜底动画，避免真实读取成功后仍被兜底动画压住。
- V1.6.5 起读取失败进入备用主页时保留约 1 秒淡出过渡，并继续放大读取框里的“鸦珉.icu”站名。
- V1.6.6 起读取失败淡出会等待完整动画结束后再移除遮罩，并给读取面板增加独立淡出和轻微上移动效。
- V1.6.7 起读取失败路径新增独立 `exiting` 淡出阶段，避免直接进入 `done` 时被隐藏状态跳过动画。
- V1.6.8 起读取失败退出改为显式 keyframes 动画，避免从兜底动画切到 transition 时被浏览器直接跳到终点。
- V1.6.9 起读取成功会保留 1.5 秒“读取完成”状态；读取框站名放大并和标题拉开层级，减少两行文字同级拥挤感。
- V1.6.10 起读取框站名和标题字号回到初始大小，只保留更紧的行距；读取失败提示停留 2 秒，让“显示备用页面”更容易被看清。
- V1.6.11 起读取框站名进一步缩小并压紧两行间距；读取成功先停留 0.5 秒，再进入 1.5 秒淡出。
- V1.6.12 起读取框站名继续缩小；“正在读取项目地图”与进度条之间加大距离，让标题更靠上、进度条更透气。
- V1.6.13 起加载遮罩显示后即开始准备云层 Canvas；线上数据或备用数据确定后，在遮罩下先渲染主页并初始化怪念头，再显示完成或失败并淡出。
- V1.6.14 起加载进度条首秒会平滑推进到 40%，随后继续匀速推进到 82%；即使数据很快成功，也会先保留首秒推进感再显示“读取完成”。
- V1.6.15 起根页面删除“项目区 1.8 秒为空就跳转 `/v1/`”的旧兜底，避免加载遮罩等待数据时误跳到 `/v1/`。
- V1.6.16 起测试服可以通过只读 Supabase 环境变量读取正式服页面数据；测试服保存和备份接口仍拒绝写入，只读变量不会被写入流程使用。
- V1.6.17 起加载遮罩一出现进度条就开始推进；进度条前 3 秒推进到 60%，如果 4 秒仍未读到数据，会先在 60% 停住再显示读取失败。
- V1.6.18 起加载进度条改为 3 秒匀速从 8% 推进到 60%，去掉第一秒冲到 40% 后中段停住的观感。
- V1.6.19 曾尝试让读取成功路径等待进度条完整跑满 3 秒；该规则在 V1.6.20 中被替换。
- V1.6.20 起读取成功后不再强制等待 3 秒，而是锁定当前读条位置并立即用约 420ms 平滑补到 100%，避免低进度处停顿后再跳到完成。
- V1.6.21 起 Canvas 云层不再跟加载遮罩同时启动；读取中阶段只保留进度条和数据读取，遮罩结束后再用空闲时间启动云层，避免第一屏进度条卡在低位后跳动。
- V1.6.22 起数据读取仍在第 0 秒开始，Canvas 云层改为加载遮罩出现后的第 2 秒安排启动；读取成功会等云层准备完成后再显示完成，读取失败会跳过云层并继续显示备用页面。
- V1.6.23 起加载遮罩改为阶段文案和脚本控制进度，测试服新增 `/fail` 强制读取失败入口，用于直接检查备用页面与失败淡出路径。
- V1.6.24 起加载文案改为固定三秒时间轴：第 1 秒读取项目地图，第 2 秒翻找待办任务，第 3 秒临时想一些怪念头；数据读取仍从第 0 秒并行开始，读取成功后再进入“正在绘制棉花糖”。

如果未来要做 3D 项目轮、留言区、账号系统等新方向，先保护这个 V1.4 主页基准，再单独规划新模块。

## 内容编辑台

V1.2 新增静态编辑台：

```txt
https://鸦珉.icu/admin
```

本地预览：

```txt
http://127.0.0.1:4173/admin
```

当前编辑台负责编辑草稿、预览、导出数据文件，并在配置保存口令后写入线上网站。

V1.25 起新增 `/api/site-data` 数据接口：

- 本地预览没有数据库配置时，保存到 `data/site-data.local.json`。
- 线上部署时，通过 Supabase REST API 写入数据库。
- Supabase 建表脚本在 `supabase/site_pages.sql`。
- Vercel 需要配置：
  - `SUPABASE_URL`
  - `SUPABASE_SERVICE_ROLE_KEY`
  - `SITE_ADMIN_TOKEN`
  - `SITE_DATA_TABLE`（可选，默认 `site_pages`）
  - `SITE_DATA_BACKUP_TABLE`（可选，默认 `site_page_backups`）
  - `SITE_DATA_ID`（可选，默认 `homepage-v1`）
  - `CRON_SECRET`（用于每周自动备份接口授权）
- 测试服如果只需要读取正式服数据，不配置上面的可写 Supabase 变量，改用：
  - `SITE_DATA_READ_SUPABASE_URL`
  - `SITE_DATA_READ_SUPABASE_KEY`
  - `SITE_DATA_READ_TABLE`（可选，默认跟 `SITE_DATA_TABLE` 或 `site_pages` 一致）
  - `SITE_DATA_READ_ID`（可选，默认跟 `SITE_DATA_ID` 或 `homepage-v1` 一致）
  - `SITE_DISABLE_ONLINE_SAVE=1`

`SUPABASE_SERVICE_ROLE_KEY` 只放在 Vercel 环境变量里，不能放到浏览器端代码里。
`SITE_DATA_READ_SUPABASE_KEY` 使用 Supabase publishable / anon key，只能读取数据库策略允许的公开主页数据，不具备写入权限；测试服当前使用的是 Supabase 项目 `ravenous-lab` 的 legacy anon key。

V1.4.1 起新增后台数据安全规则：

- 后台编辑台如果没有读到线上数据库，只读到静态旧数据，会禁止直接保存，避免旧内容覆盖线上数据。
- 线上保存前会先把当前数据库内容写入 `site_page_backups`，保存后再写入一份新快照。
- `/api/site-data-backup` 可用于手动或定时备份当前线上内容。
- `vercel.json` 配置每周自动调用一次 `/api/site-data-backup`，生产环境需要设置 `CRON_SECRET`。

V1.4.2 起新增后台会话规则：

- 后台保存不再把口令或口令哈希存入 `localStorage`。
- 输入后台口令后，服务器发放 HttpOnly cookie；普通设备默认 3 小时有效。
- 授权弹窗可勾选“信任这台设备”，可信设备会话 180 天有效，并在打开后台或保存时自动续期。
- 服务端只在 `admin_sessions` 表保存 session token 的哈希、过期时间和可信设备标记。
- 会话过期后再次保存会提示重新输入口令，当前草稿不会丢。

V1.27 调整后台编辑体验：
- 修复项目卡片、待办横条的上移 / 下移排序。
- 待办横条增加黄、青、黑、白等颜色，并把红色改为更明确的大红。
- 奇怪念头编辑项改为紧凑标签式，只保留文本、显示开关和删除。

V1.28 调整主页待办区：
- 待办区改为标题 + 内部列表布局。
- 6 条待办可以正常显示在右上模块内。
- 超过可用高度时只在待办列表内部滚动，不挤压右下账号区。

V1.29 调整主页项目区：
- 项目区前两张大卡列宽略微收窄。
- 右侧横向项目卡列略微放宽，让项目区比例更轻。

V1.30 调整奇怪念头区：
- 奇怪念头从固定位置标签升级为“念头空间”。
- 标签拥有随机大小、随机初始位置、随机速度和方向。
- 标签在空间内边界反弹，彼此碰撞后弹开。
- 标签每隔 3 到 5 秒随机出现或隐藏；隐藏后不参与碰撞。
V1.31 修正待办区：
- 白色待办标签的进度条改为浅白底轨 + 白色高亮填充，避免显示成灰色进度条。
V1.32 修正待办区：
- 白色待办标签不再使用单独的浅白轨道样式。
- 待办进度条统一为淡蓝底轨，进度填充跟随标签颜色。
V1.33 调整奇怪念头区：
- 奇怪念头标签颜色改为前台自动洗牌分配，不再依赖后台数据中的固定颜色。
- 即使后台新增念头默认颜色都是蓝色，前台也会尽量均匀分布为多种颜色。
- 后台保存念头时不再记录颜色字段，颜色只作为每次展示时生成的临时状态。
V1.34 调整页面文案与奇怪念头区：
- 浏览器标题去掉 `V1`，显示为“鸦珉.icu - 项目地图”。
- 站点签名副标题改为“一张不太正经的项目地图”。
- 奇怪念头空间的碰撞区域向上下和横向扩大，让标签活动范围更接近底部区域。
V1.35 调整奇怪念头区：
- 奇怪念头标签的边界反弹安全距离从 `8px` 增加到 `40px`。
- 标签和阴影会在接近碰撞边缘前反弹；空间较窄时会自动取可用的最大安全距离，避免阴影被裁切后显出边界感。
V1.36 修正手机端内容空白：
- 首页公共脚本去掉 `replaceAll`、`??`、`?.`、`catch {}` 等旧手机浏览器容易不兼容的写法。
- 页面会先填充项目、待办、念头和频道内容，再尝试启动云层 Canvas 动画；云层失败不会再阻断内容显示。
- 根页面增加 `nomodule` 兜底，不支持模块脚本的浏览器会进入带静态内容的 `/v1/` 页面。
V1.37 调整奇怪念头区：
- 奇怪念头碰撞空间扩大到接近左下区域整块宽度，从左侧较近位置开始，到右侧边界前结束。
- 奇怪念头区标题上移一点，并保持在念头标签上方，避免标签经过时压住标题。
V1.38 继续扩大奇怪念头区：
- 奇怪念头碰撞空间继续向上、向下扩大，让标签活动范围更接近左下整块区域。
- 奇怪念头区标题再上移一点，给标签运动留出更多低区空间。
V1.39 微调奇怪念头区：
- 奇怪念头碰撞空间继续向上下贴近面板边缘，进一步扩大标签活动范围。
- 奇怪念头区标题回到 V1.37 的位置，不再继续上移。
- 页面刷新时奇怪念头初始只随机显示 5-6 个，之后在这个数量附近慢慢出现或消失。
V1.3.10 调整频道入口与版本命名：
- 右下角频道入口标签改为跟右上角待办横条一致的宽度节奏，减少底部标签偏长的突兀感。
- 当前小版本命名改为三段式 `V1.3.10`，后续用 `主版本.功能阶段.小版本序号` 记录。
V1.3.11 调整移动端性能与念头数量：
- 奇怪念头初始可见数量从 5-6 个提高到 7-8 个。
- 手机和低性能设备不再启动重 Canvas 云层，改用现有 SVG 云层兜底，避免页面加载后卡住。
- 根页面增加模块脚本兜底跳转，模块内容未填充时自动进入带静态内容的 `/v1/`。

V1.3.12 调整首屏加载与性能：
- 浏览器标题改为 `鸦珉.icu`，去掉标题里的“项目地图”后缀。
- 首页增加轻量顶部进度条，用于提示本地内容、线上数据和动画启动的加载阶段。
- 线上数据请求设置短超时，念头物理动画和 Canvas 云层延后到内容出现后再启动；手机和低性能设备不再运行念头物理动画。

V1.4.0 主页视觉定稿：
- 主页升级为固定 2:1 设计画布 + 外层整体等比例缩放，桌面小窗口下不再让内部卡片和文字独立挤压变形。
- 项目区、待办区、频道入口、奇怪念头区完成多轮比例微调，当前视觉作为后续版本基准。
- 奇怪念头区修正小窗口碰撞范围、右边界文字换行、活动边界和随机大小，保持更自然的漂浮碰撞感。
- 新增 V1.4 视觉基准说明，后续非重设计任务不要轻易改动主页骨架、卡片比例和核心动效参数。

V1.4.1 后台保存保护与数据备份：
- 后台读不到线上数据库时禁止保存，避免静态旧数据覆盖线上。
- 每次线上保存前后写入 `site_page_backups`。
- 新增每周备份接口和 Vercel Cron 配置。

V1.4.2 后台会话与可信设备：
- 后台口令不再存入 `localStorage`。
- 普通设备会话 3 小时，可信设备会话 180 天。
- 服务端只保存 session token 哈希和过期时间。

V1.4.3 主页小调：
- 右上标题改为“施工中”，左下标题改为“怪念头”。
- 怪念头碰撞上边界向上扩展，并与下边界保持同样外扩量；右下频道入口条再变矮。
- 项目区横向空白卡片标题字号小 1px。

V1.4.4 后台编辑基准版本校验：
- `/api/site-data` 读取内容时返回当前数据 revision。
- 后台保存线上内容时提交打开编辑台时的 `expectedRevision`；服务器发现 revision 不一致时返回冲突并停止写库。
- 后台新增“查看主页”入口，使用新标签打开主页，减少通过浏览器后退来回切换导致旧状态保存的风险。
- 后台从浏览器历史缓存恢复时会重新检查线上 revision；发现旧版本时保留当前草稿并禁止直接覆盖。

## 检查

```txt
npm.cmd run check
```

## 进度文档

阶段记录放在：

```txt
项目进度/
```

当前重点文档：

```txt
项目进度/2026-06-01_V1上线状态与下一阶段计划.md
项目进度/2026-06-01_版本命名规则.md
项目进度/2026-06-03_V1.4.0主页视觉定稿与固定规则.md
项目进度/2026-06-03_V1.4.1后台保存保护与数据备份.md
项目进度/2026-06-03_V1.4.2后台会话与可信设备.md
项目进度/2026-06-04_V1.4.3主页标题与念头边界小调.md
项目进度/2026-06-04_V1.4.4后台编辑基准版本校验.md
项目进度/2026-06-05_测试服Vercel项目与域名绑定记录.md
项目进度/2026-06-05_V1.5.8主页线上数据读取容错.md
项目进度/2026-06-05_V1.5.9读取失败表情包背景.md
项目进度/2026-06-08_V1.6加载遮罩与测试服预览.md
项目进度/2026-06-08_V1.6.1读取失败状态与透明度小调.md
项目进度/2026-06-08_V1.6.2加载失败等待与站名放大.md
项目进度/2026-06-08_V1.6.3失败提示快速交接备用主页.md
项目进度/2026-06-08_V1.6.4加载兜底动画正常释放.md
项目进度/2026-06-08_V1.6.5加载失败淡出与站名放大.md
项目进度/2026-06-08_V1.6.6加载失败淡出完整等待.md
项目进度/2026-06-08_V1.6.7加载失败独立淡出阶段.md
项目进度/2026-06-08_V1.6.8加载失败显式退出动画.md
项目进度/2026-06-08_V1.6.9读取成功停留与站名层级.md
项目进度/2026-06-08_V1.6.10加载字重回退与失败提示停留.md
项目进度/2026-06-08_V1.6.11读取成功淡出与站名小调.md
项目进度/2026-06-08_V1.6.12加载标题间距与站名缩小.md
项目进度/2026-06-08_V1.6.13遮罩下预备主页和怪念头.md
项目进度/2026-06-08_V1.6.14加载进度条首秒推进.md
项目进度/2026-06-08_V1.6.15根页面不再误跳V1.md
项目进度/2026-06-08_V1.6.16测试服只读正式数据.md
项目进度/2026-06-08_V1.6.17加载进度条立即启动.md
项目进度/2026-06-08_V1.6.18加载进度条匀速推进.md
项目进度/2026-06-08_V1.6.19读取成功进度条完整推进.md
项目进度/2026-06-08_V1.6.20读取成功立即平滑补满.md
项目进度/2026-06-08_V1.6.21云层Canvas延后启动.md
项目进度/2026-06-08_V1.6.22云层Canvas第二秒启动.md
项目进度/2026-06-09_V1.6.23阶段加载文案与失败入口.md
项目进度/2026-06-09_V1.6.24固定三秒加载文案.md
```
