# 项目协作入口

文档版本：V1

## 长期工作习惯

- 用户常用语音输入，需求文字可能有错别字；执行前应结合上下文判断真实意图。
- `AGENTS.md` 与 `Memory.md` 只记录长期有效的工作习惯、操作顺序、归档顺序和输出偏好，不记录当天任务历史、试错过程或临时备注。
- 默认入口配置文件使用固定文件名，例如 `AGENTS.md`、`Memory.md`；版本号写在文件内部，不用带版本号文件作为最终生效入口。
- 修改默认入口配置文件前，如果原文件存在，必须先备份到 `backup/` 目录，文件名包含版本、日期和原因。

## 工作流文档

- 最新发布工作流文档：`Website_Update_Workflow.md`
- 项目进度与版本记录放在：`项目进度/`

## 发布规则

- 常规上线顺序必须是：先提交并推送到 GitHub，再由 Vercel 根据 GitHub 仓库自动部署。
- 不要把本地未提交工作区直接作为常规方式部署到 Vercel。
- 如果因临时验证直发 Vercel，必须明确说明 GitHub 尚未同步，并尽快提交和推送到 GitHub。

## 文件操作

- 禁止批量删除文件或目录。
- 不要使用 `del /s`、`rd /s`、`rmdir /s`、`Remove-Item -Recurse`、`rm -rf`。
- 需要删除文件时，只能一次删除一个明确路径的文件。
