# V1.3.10 频道标签宽度与版本命名记录

记录时间：2026-06-02

## 背景

用户反馈右下角频道入口标签比右上角待办横条更长，看起来不协调；同时希望后续版本命名改成更严谨的三段式，例如 `V1.3.10`。

## 本次调整

- 右下角频道入口标签改为跟待办横条一致的列宽、内边距、最小高度和点状色标尺寸。
- 首页和 `/v1/` 静态资源缓存版本更新为 `1.3.10-social-width`。
- 后台角标更新为 `V1.3.10`。
- README 当前阶段更新为 `V1.3`，当前小版本更新为 `V1.3.10`。
- 更新 `项目进度/2026-06-01_版本命名规则.md`，从 `V1.3.10` 起采用三段式命名。

## 线上数据修正

确认线上 API 从 Supabase 返回的 `identity.subtitle` 仍是旧文案：

```txt
一张稍微不太正经的项目地图
```

已将 Supabase `site_pages / homepage-v1` 中的副标题更新为：

```txt
一张不太正经的项目地图
```

## 检查

已执行：

```txt
npm.cmd run check
```

结果：34 项测试全部通过。

## 注意

- 未修改 `AGENTS.md` / `Memory.md`。
- 未删除任何文件。
