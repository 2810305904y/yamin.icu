# V1.6.12 加载标题间距与站名缩小
文档版本：V1

## 版本目标

- 读取框内 `鸦珉.icu` 再小一点，减少和主标题的视觉抢占。
- “正在读取项目地图”这一行和进度条之间拉开距离，让标题视觉上更靠上。
- 不改变 V1.6.11 已确认的读取成功节奏：显示“读取完成”约 0.5 秒后进入 1.5 秒淡出。
- 不改变读取失败路径：4 秒读取中、失败提示停留 2 秒、随后淡出进入备用页面。

## 调整记录

- 读取框站名字号从 `24px` 调整为 `22px`。
- 进度条上边距从 `4px` 调整为 `12px`。
- 资源版本号更新为 `1.6.12-loading-title-space`，避免测试服继续缓存旧样式和旧脚本。

## 检查状态

- 本地检查：`npm.cmd run check` 已通过。
- 测试服部署：已部署到 `https://test.xn--idyr71g.icu/`。
- 线上验证：根页面、CSS、主页脚本和 `/api/site-data` 已确认返回 V1.6.12 相关资源；测试服 API 仍为静态回退数据。
- 正式服部署：已发布到 `https://xn--idyr71g.icu/`。
- 正式服验证：根页面、CSS、主页脚本和 `/api/site-data` 已确认返回 V1.6.12 相关资源。
