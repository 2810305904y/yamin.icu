# V1.6.10 加载字重回退与失败提示停留

文档版本：V1

## 版本目标

- 读取框内 `鸦珉.icu` 和读取标题恢复到初始字号。
- 保留更紧的两行间距，避免文字上下距离过散。
- 读取失败后额外停留，让“读取失败，显示备用页面”更容易被看清。

## 调整记录

- 读取框站名恢复为 `28px`，不再使用 V1.6.9 的放大和渐变文字。
- 读取标题恢复为 `28px`，行高收紧到 `1.12`。
- 读取失败提示停留时间调整为 `2000ms`，成功路径不增加这段等待。
- 资源版本号更新为 `1.6.10-failure-hold-compact-brand`，避免测试服继续缓存旧样式。

## 检查状态

- 本地检查：`npm.cmd run check` 已通过。
- 测试服部署：已部署到 `https://test.xn--idyr71g.icu/`。
- 线上验证：根页面、CSS、主页脚本和 `/api/site-data` 已确认返回 V1.6.10 相关资源；测试服 API 仍为静态回退数据。
