# 2026-06-02 V1.32 待办白色进度条统一规则记录

## 版本定位

V1.32 是 V1.2 阶段的视觉小修版本，修正 V1.31 对白色待办进度条的理解偏差。

## 本次确定内容

- 白色待办标签不再使用单独的浅白底轨样式。
- 待办进度条统一为淡蓝底轨。
- 进度填充统一使用 `currentColor`，因此白色待办的进度填充为白色，红色、蓝色、黑色等仍跟随各自标签颜色。
- 首页与 V1 页面资源版本号更新为 `1.32-white-todo-standard`，避免浏览器继续使用旧样式缓存。

## 验证

- `npm.cmd run check` 已通过，29 个测试全部通过。
- 本地预览 `http://127.0.0.1:4181/?v=1.32-white-todo-standard` 已确认：
  - 页面加载后不存在 `.todo-tone-white .todo-track` 专属覆盖。
  - 待办底轨统一为淡蓝轨道。
  - 待办进度填充统一使用 `currentColor`。
