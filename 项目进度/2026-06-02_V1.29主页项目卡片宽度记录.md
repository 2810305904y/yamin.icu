# 2026-06-02 V1.29 主页项目卡片宽度记录

## 版本定位

V1.29 是 V1.2 阶段的小视觉修复版本，重点是微调主页项目区大卡的比例。

## 本次确定内容

- 项目区前两张大卡从 `1fr / 1fr` 收窄为 `0.94fr / 0.94fr`。
- 右侧横向项目卡列从 `1.28fr` 放宽为 `1.4fr`。
- 项目区整体仍保持三列布局和当前宽屏框架，不改变右侧待办区与下方区域。
- 根页面和 `/v1` 页面样式版本更新为 `1.29-project-card-trim`，避免线上缓存旧样式。

## 验证

- `npm.cmd run check` 已通过。
- 视觉目标：项目区大卡略微变瘦，不再显得过肥。
