# 测试服 Vercel 项目与域名绑定记录

日期：2026-06-05

## 目标

为个人主页建立独立测试服，后续手机端、3D 项目区、新交互和未定稿视觉先进入测试服确认，再合并到主站。

## 测试服地址

目标中文域名：

```txt
https://test.鸦珉.icu/
```

Punycode 形式：

```txt
https://test.xn--idyr71g.icu/
```

Vercel 临时地址：

```txt
https://yamin-icu-test.vercel.app/
```

## 已完成

- Vercel 已创建独立测试项目：`yamin-icu-test`。
- 当前代码已部署到测试项目，临时地址已能返回 `V1.5.7` 页面。
- `test.xn--idyr71g.icu` 已添加并绑定到测试项目。
- 正式测试域名 `https://test.xn--idyr71g.icu/` 已验证可访问。
- 本地 `.vercel` 链接已恢复到主站项目：`yamin-icu`。
- 测试项目当前没有配置环境变量，后台保存不会连到主站数据库。
- 测试服 `/admin` 已禁用“保存到线上”；后端 `/api/site-data` 在测试服域名下也会拒绝写入。

## 待完成

- 暂无必须项。
- 后续如果测试服需要后台保存能力，应单独规划独立数据、独立环境变量和独立 `SITE_DATA_ID`，再解除测试服保存禁用。

## 后续发布规则

- 主站 `鸦珉.icu` 只放稳定版。
- 测试服 `test.鸦珉.icu` 用于未定稿内容预览。
- 测试服确认满意后，再提交并推送到主站流程。

## 后续测试服部署顺序

临时把本地目录指向测试项目：

```txt
npx.cmd vercel link --yes --project yamin-icu-test --scope ravenous-projects-47c09001
```

部署到测试项目：

```txt
npx.cmd vercel --prod --yes --scope ravenous-projects-47c09001
```

部署后必须把本地目录重新指回主站项目：

```txt
npx.cmd vercel link --yes --project yamin-icu --scope ravenous-projects-47c09001
```
