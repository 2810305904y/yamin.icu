# V1.6.5 加载失败淡出与站名放大

日期：2026-06-08

## 目标

让读取失败进入备用主页时不再显得突然，同时继续放大读取框里的“鸦珉.icu”站名。

## 调整

- 成功读取路径保持 V1.6.4 的快速释放逻辑，不再被 8.2 秒兜底动画压住。
- 读取失败路径进入 `done` 状态时，单独设置约 1 秒淡出过渡，让备用主页慢慢露出。
- 读取框内的“鸦珉.icu”站名字号从 `22px` 放大到 `28px`。
- 页面资源号更新为 `1.6.5-failure-fade-and-brand`，避免浏览器继续使用旧样式。

## 状态

- 本地测试：已通过。
- 测试服：已部署并验证。
- 主站：待确认后再部署。
