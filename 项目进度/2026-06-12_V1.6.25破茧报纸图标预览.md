# V1.6.25 破茧报纸图标预览

日期：2026-06-12

## 目标

- 将“破茧”项目卡片里语义不清的 `leaf` 抽象图标替换为更贴近内容产品的报纸线性图标。
- 保持图标风格与当前蓝色线框 UI 一致，不引入位图资源，避免清晰度和加载问题。
- 兼容线上数据中已有的 `leaf` 字段值，避免测试服只读正式数据时仍显示旧图标。

## 行为

- 新增 `newspaper` 图标，实际展示使用前面生成的报纸 PNG 原图 `v1/assets/pojian-newspaper-icon.png`。
- `leaf` 图标值保留为兼容别名，但渲染结果改为同一张报纸图。
- 默认内容里的“破茧”项目图标从 `leaf` 改为 `newspaper`。
- 后台图标选项新增 `newspaper`，旧 `leaf` 仍暂时保留用于兼容历史数据。

## 状态

- 本地代码已更新为 V1.6.25。
- 本地测试覆盖新增图标、旧 `leaf` 兼容和缓存标记。
- 已临时直发部署到测试服 `https://test.xn--idyr71g.icu/` 供确认。
- 测试服上一版验证：首页返回 `1.6.25-pojian-newspaper-icon` 缓存标记；主页脚本包含 `newspaperIcon` 和 `leaf: newspaperIcon`，不再包含旧叶子路径。
- 根据测试反馈，手写 SVG 与生成图差异过大，已改为直接使用生成图片并更新缓存标记为 `1.6.25-pojian-newspaper-image`。
- 新测试服验证：首页返回 `1.6.25-pojian-newspaper-image` 缓存标记；主页脚本使用 `/v1/assets/pojian-newspaper-icon.png`；图片资源返回 200。
- 根据测试反馈，生成图里的假透明棋盘格已处理为真实透明 PNG，并给图片 URL 增加 `v=1.6.25-transparent-bg` 缓存参数。
- 本轮页面缓存标记更新为 `1.6.25-pojian-newspaper-transparent`。
- 测试服已重新部署并验证：`https://test.xn--idyr71g.icu/` 返回透明版缓存标记；脚本使用透明图缓存参数；图片资源返回 200，大小约 295KB。
- 用户已确认图标效果，V1.6.25 按 GitHub-first 流程同步正式服。
- 正式服验证目标：`https://xn--idyr71g.icu/` 返回 `1.6.25-pojian-newspaper-transparent` 缓存标记，报纸图片资源返回 200。
